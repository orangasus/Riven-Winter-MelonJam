rules:
please add comments that describe what your code does so it can be more understandable
eg- what does a method input, output, and do behind the scenes.
files are all lowercase, first letter of class names are capitalized
below we will list the classes and functionalities and who makes them



classes:

Tile:
variables = [size, position, is_real, is_lethal, is_climbable, sprite, sound_effect]

functions:
function1(inputs:)
outputs:

Object
variables:

functions:
function1(inputs:)
outputs:

class 2
variables:

functions:
function1(inputs:)
outputs:

class 3
variables:

functions:
function1(inputs:)
outputs:
